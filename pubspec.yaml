name: retro_workbench
description: The shared workbench GUI the Retro-* family inherits, runnable on its own - design here, copy verbatim into the apps.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
